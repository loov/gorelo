# Move an iota-dependent const (warning about value change).

Blue -> colors_out.go

@warn const Blue depends on iota

-- input/go.mod --
module example.com/pkg

go 1.21
-- input/colors.go --
package mypkg

type Color int

const (
	Red Color = iota
	Green
	Blue
)

func OtherColor() {}
-- golden/colors.go --
package mypkg

type Color int

const (
	Red Color = iota
	Green
)

func OtherColor() {}
-- golden/colors_out.go --
package mypkg

const Blue
