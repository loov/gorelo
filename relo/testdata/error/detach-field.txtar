# Error: @detach requires a method. A struct field cannot be detached.

Widget#Name=!NewName

@error @detach requires a method

-- input/go.mod --
module example.com/pkg

go 1.21
-- input/main.go --
package mypkg

type Widget struct {
	Name string
}
