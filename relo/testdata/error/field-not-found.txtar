# Error: renaming a struct field that does not exist.

Widget#DoesNotExist=NewName

@error could not find field "DoesNotExist" in struct "Widget"

-- input/go.mod --
module example.com/pkg

go 1.21
-- input/main.go --
package mypkg

type Widget struct {
	Name string
}
