# Error: moving an unexported identifier to a different package.

uuid_ -> dst/ids.go

@error unexported name "uuid_" cannot be moved cross-package without a rename

-- input/go.mod --
module example.com/test

go 1.21
-- input/src/source.go --
package src

var uuid_ = "default-uuid"

func helper() string {
	return "internal"
}
