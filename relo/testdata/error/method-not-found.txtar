# Error: detaching a method that does not exist on the type.

Server#DoesNotExist=!

@error could not find method "DoesNotExist" on type "Server"

-- input/go.mod --
module example.com/pkg

go 1.21
-- input/main.go --
package mypkg

type Server struct{}

func (s *Server) Start() {}
