# Error: @attach requires a function. A type cannot be attached as a method.

MyType=Server#Helper

@error @attach requires a function

-- input/go.mod --
module example.com/pkg

go 1.21
-- input/main.go --
package mypkg

type Server struct{}

type MyType struct{}
